import { DrawableObject } from "./drawable-objects.class.js";

/**
 * creates collectable objects as in bottles and coins
 * @class
 */
export class CollectableObjects extends DrawableObject {
    throwObj = false;
}
